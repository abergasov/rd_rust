cli run

```bash
make run
```

user input is block thread, so to correct exit from the program, use `Ctrl+C` and extra letter :)
or `process::exit(0);`

sample usage:

```bash
l ADWADWA
u adwadawd
csv 1
```